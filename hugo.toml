baseURL = "https://packslip.dev/"
locale = "en-US"
title = "packslip"
disableKinds = ["rss", "sitemap", "taxonomy", "term"]

[markup]
  [markup.highlight]
    noClasses = false

[caches]
  [caches.images]
    dir = ":cacheDir/images"
